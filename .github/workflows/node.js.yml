name: main Branch 

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'  

    - name: Install dependencies
      run: npm install 

    - name: Build project
      run: npm run build

    # - name: Source movement
    #   run: sudo cp -r /home/administrator/Live-Project/Live_Front/rehana/_work/Rehana__Live/Rehana__Live/dist/ * /var/www/html/rehana

    # - name: Nginx restart
    #   run: sudo service nginx restart
